#include <iostream>
using namespace std;
int main() {
	setlocale(0, "");
	short kolvo;
	cout << "Введите число: ";
	cin >> kolvo;
	if (kolvo <= 15 && kolvo > 0) {
		do {
			cout << "kolvo" << endl;
			kolvo = kolvo - 1;
		} while (kolvo != 0);
	}
	else cout << "Ошибка, попробуй снова";
}
